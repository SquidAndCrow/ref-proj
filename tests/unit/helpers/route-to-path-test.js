import { routeToPath } from 'reference/helpers/route-to-path';
import { module, test } from 'qunit';

module('Unit | Helper | route to path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routeToPath([42]);
  assert.ok(result);
});
